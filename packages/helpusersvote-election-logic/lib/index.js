const { getStates, getState } = require('./data')
const { hasRegPassed, shouldShowCTA, getCountdown } = require('./logic')

module.exports = {
  hasRegPassed,
  shouldShowCTA,
  getCountdown,
  getStates,
  getState
}
